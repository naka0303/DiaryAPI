<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.DiariesQueryMapper">
    <select id="findDiariesByUserId" resultType="com.example.demo.domain.diariesContents.entity.DiariesContents">
        SELECT
		    d.diary_id
			,  d.user_id
			,  c.diary_title
			,  c.diary_content
        FROM
        	diaries d
		LEFT JOIN
		    contents c
	    ON
		    d.diary_id = c.diary_id
		WHERE
		    d.user_id = #{userId}
    </select>
    
    <select id="findUsers" resultType="com.example.demo.domain.users.entity.Users">
        SELECT
		    u.user_id
			,  u.username
			,  u.age
			,  u.email
			,  u.auth
        FROM
        	users u
    </select>
    
</mapper>